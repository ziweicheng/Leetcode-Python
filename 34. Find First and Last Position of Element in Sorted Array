class Solution:
    def searchRange(self, nums: List[int], target: int) -> List[int]:
        if not nums:
            return [-1, -1]
        n = len(nums)
        left = 0
        right = n-1
        res = []
        while left < right:
            mid = (left + right) // 2
            if nums[mid] >= target:
                right = mid
            else:
                left = mid + 1
        if nums[left] == target:
            res.append(left)
        else:
            return[-1, -1]
        left = 0
        right = n - 1
        while left < right:
            mid = (left + right) // 2
            if nums[mid] > target:
                right = mid
            else:
                left = mid + 1
        if nums[left] == target:
            res.append(left)
        else:
            res.append(left - 1)
        if not res:
            return [-1, -1]
        else:
            return res
